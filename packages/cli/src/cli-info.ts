/**
 * cli-info.ts —— CLI 标识常量。
 */

export const CLI_NAME = "pomaster" as const;

/** CLI 版本锚（与 packages/cli/package.json version 同步；init 账本 tool 锚另见 digest.ts）。 */
export const CLI_VERSION = "0.0.0" as const;
